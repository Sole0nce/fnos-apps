自动构建的 fnOS 安装包

- 基于 [GSManager v${VERSION}](https://github.com/GSManagerXZ/GameServerManager/releases/tag/v${VERSION})
- 平台: fnOS (Docker)
- 默认端口: ${DEFAULT_PORT} (Web UI), 27015 (游戏)${REVISION_NOTE}
- 数据目录: ${TRIM_PKGVAR}（含 home/ 与 root/ 游戏数据）
${CHANGELOG}
**国内镜像**:
- [${FILE_PREFIX}_${VERSION}_x86.fpk](https://ghfast.top/https://github.com/Sole0nce/fnos-apps/releases/download/${RELEASE_TAG}/${FILE_PREFIX}_${VERSION}_x86.fpk)
