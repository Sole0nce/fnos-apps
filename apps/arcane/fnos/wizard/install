[
    {
        "stepTitle": "安装说明",
        "items": [
            {
                "type": "tips",
                "helpText": "Arcane 是现代化的 Docker 管理界面，可管理容器、镜像、Compose 项目、存储卷与网络。<br><br><b>运行原理：</b>本应用通过挂载宿主机 <code>/var/run/docker.sock</code> 管理 Docker，拥有与 Docker 守护进程等同的权限，请妥善保管访问账号。"
            },
            {
                "type": "tips",
                "helpText": "<b>镜像说明：</b>应用镜像 <code>ghcr.io/getarcaneapp/manager</code> 托管在 GitHub Container Registry，支持 amd64 与 arm64。<br>Docker「镜像加速器」通常仅对 Docker Hub 生效，<b>对 ghcr.io 可能不生效</b>；如拉取失败，请确保 NAS 能直连 ghcr.io。"
            },
            {
                "type": "tips",
                "helpText": "安装向导中填写的加密密钥与 JWT 密钥会在创建容器前由 fnOS 直接替换进 docker-compose 环境变量；本应用不使用 .env / env_file。"
            }
        ]
    }
]
