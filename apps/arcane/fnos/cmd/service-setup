#!/bin/bash
# Docker mode: container is created by fnOS from docker-compose.yaml.
# Only ensure the persistent data directory exists.

service_postinst() {
    mkdir -p "${TRIM_PKGVAR}/data" 2>/dev/null || true
}
